CHANNEL:
    FIELDS: ChannelSize, ChannelFillPercentage, EventPutAttemptCount

SOURCE:
  FIELDS: EventAcceptedCount,EventReceivedCount

SINK:
  FIELDS: ConnectionCreatedCount, ConnectionClosedCount, EventDrainSuccessCount, BatchUnderflowCount
